export default function WalletOverview() {
  return (
    <div style={{ padding: 24 }}>
      <h1>Wallet Overview (Engine)</h1>
      <p>Show Naira + BidCredit balances here (default 0 until API integration).</p>
    </div>
  );
}
